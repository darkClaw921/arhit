# docSearchCommand

Команда arhit doc search <query>. Ищет по полям: element, content, path, а также по массиву aliases[]. Если результатов нет — возвращает []. В human-режиме показывает список алиасов рядом с найденным элементом.
